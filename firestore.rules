rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODO TEST (desarrollo) — sin auth.
    // Cambiar a reglas reales antes de producción.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
